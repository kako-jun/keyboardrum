# keyboardrum — DESIGN

## コンセプト

PCで文字を打つたびに、入力のついでにドラムが鳴る常駐アプリ。キーボードの領域ごとに
ドラムの各パーツを割り当て（スペース＝足＝キック、右上＝シンバル等）、普段の仕事の
タイピングがそのままドラム演奏になる。

## 製品像（二層）

- **トイ**（全タイピスト）：即バズ・楽しい。元の軸。
- **アクセシビリティ**（全盲ユーザ）：普通のキーボードがそのまま楽器になる＝MIDI購入のリスク無しで
  「音楽が楽しいか」を試せる。ドラムは音だけで成立・音階なし＝視覚ゼロで完結。esuna 連携（#8）。
- 入力ルーターは1本、出力を差し替え＝**OutputProvider（drum / speech / both）**（#2）。
  speech は打鍵を可愛い声で字名読み上げ＝全盲タイピストの打鍵補助（#9）。

## なぜ噛み合うか

- ドラムは**音階が無い**＝「外す」概念が存在しない。何を叩いても破綻しない。
- PCキーボードは**基本モノフォニック（1個ずつ押す）**＝1打＝1ヒットに素直。
- 音階楽器なら「和音／運指」で破綻する制約が、ドラムでは全部消える。

## 領域マッピング

頻度の高いキー（ホーム段）ほど刻みの音（スネア／ハット）、稀なキー（数字・記号・Enter）ほど
シンバル。普通に打つだけでドラムらしい疎密が出るのが狙い。割り当ては `frontend/index.html`
の `ZONES` を単一の真実とする。

## 発音ロジック（重要）

2つの別ルールを両立：

1. **ポリフォニー**：異なるパーツの同時押し（キック＋スネア）は両方鳴らす。
2. **同一パーツの合流**：同一パーツに当たるキーを2つ同時に押しても1回だけ鳴らす。
   - 実装：`lastHit[piece]` に最終発音時刻を保持。同一パーツが `COALESCE_MS`(=14ms) 内に
     再来したら無視。異なるパーツは各自の時刻なので独立に同時発音できる。
3. **オートリピート無視**：`keydown.repeat` を捨て、1物理押下＝1打。`keyup` で再武装。

## 音色

- **本番採用：Web Audio の手続き合成**（kako-jun 決定。kick=ピッチ落ちsine, snare=tone+noise,
  hat=highpass noise…）。ライセンス無縁・数十KB・**揺らぎがタダ**（合成なら音程/減衰/音色を1打ごとに
  連続ランダム化＝無限ラウンドロビン）。キーボードに強弱が無い「機関銃化」はこの揺らぎで回避済み。
- 任意の後回し：生ドラム（アコースティック）サンプルを **optional な音色パック**として（#1）。
  - 調達：AVL Drumkits（`.h2drumkit`, 5レイヤー＋複数インスタンス）等。
  - ライセンス：AVL は GPL/CC-SA。コピーレフトは音資産に付きコードに伝染しないので、本体 MIT のまま
    パックを各ライセンス＋クレジットで同梱できる。
- 低レイテンシ必須（<20ms目標）。

## ステージ

1. **フォーカス専用 Web/Tauri 試作**（現状）。音色・領域マップ・発音ロジック・刻みの気持ちよさを詰める。
2. **常駐ネイティブ**：Rust 側でグローバルキーフックを足し、全アプリで鳴らす。
   - Wayland は全キー盗聴を原則禁止 → X11 / evdev 直読み（input グループ）/ macOS Accessibility /
     Windows 低レベルフック、のプラットフォーム別実装。

## ビルド・配信（1フロント・2ターゲット）

Tauri は「Web アプリを native の殻で包む」もの。`frontend/` が Web アプリの実体、`src-tauri/` は
その**ビルド出力を消費して**デスクトップアプリに包むだけ。1フォルダで同居する。

- **Web サイト**：`frontend/` をそのまま静的ホスティング（Cloudflare Pages / `keyboardrum.llll-ll.com`）。
  `npm run build` → 静的出力をデプロイ（#6）。
- **デスクトップアプリ**：`npm run tauri:build` で**同じ frontend** を native バイナリに同梱（#4・GitHub Releases）。
- `tauri.conf.json` の `frontendDist` が frontend の出力を指すだけ＝二重実装しない。

### プラットフォーム差は1箇所（InputAdapter）に閉じ込める

Web ブラウザに Tauri の native 機能（グローバルフック・トレイ・`@tauri-apps/api`）は無い。判定して分岐：

- 判定：`'__TAURI_INTERNALS__' in window`（Tauri v2）。
- **web アダプタ**：フォーカス時 `keydown` だけ（現状）。
- **Tauri アダプタ**：Rust グローバルフックを足す（#4）。

これは `InputAdapter`（#2）の置き場そのもの。共有エンジン（発音ロジック・音色）は触らない。

### 運用の勘所

- **CI を分ける**：Web デプロイ（Pages）は frontend だけ＝Rust 不要。Tauri ビルドは OS別ランナーで別ワークフロー。
- **相対パス**：`tauri://localhost` でも subdomain でもアセットは相対参照で両対応。
- **`@tauri-apps/api` は判定ガード or 動的 import**：web バンドルで未ガード実行すると落ちる。`inTauri` の中だけで読む。

## 今後の候補

- ラウンドロビン用の生ドラムサンプル取り込み（`.h2drumkit` パース）。
- グルーヴロック（量子化）モード：BPMを決めて打鍵を16分グリッドに吸着。
- ビジュアルの作り込み（キット図の発光、ティッカー）。
